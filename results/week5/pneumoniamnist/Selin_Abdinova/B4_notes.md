UMAP visualization of the SSL encoder features shows partial clustering of
pneumonia and normal chest X-ray images.

Although the classes are not perfectly separated, there is a visible grouping
pattern indicating that the encoder learned meaningful visual representations
without using labels.

These representations can be effectively used for downstream classification
tasks with limited labeled data.
